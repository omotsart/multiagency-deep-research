# Deep Research — зависимости
# Версии зафиксированы намеренно (страховка от несовместимостей, особенно UI на Spaces).
# Перед деплоем сверь актуальные стабильные версии и обнови при необходимости.

# OpenAI Agents SDK (импортируется как `agents`), требует Python 3.10+
openai-agents>=0.2,<1.0

# UI — версия зафиксирована во избежание "перекошенного" интерфейса на новых релизах
gradio==5.49.1

# Валидация структурированного вывода агентов
pydantic>=2.0,<3.0

# Загрузка .env локально
python-dotenv>=1.0

# Email (в деплое не используется; оставлено для локальной демонстрации email-агента)
sendgrid>=6.0

# Тесты (моки LLM, без реальных вызовов)
pytest>=8.0
pytest-asyncio>=0.23
