# Handover — Gate 5: UI (три экрана)

## Тип
[x] Открытие gate   [ ] Закрытие gate   [ ] Перенос сессии (gate не закрыт)

**Статус открытия.** Gate 4 закрыт (HEAD `9c8ade6`, журнал закрытия в WORKFLOW.md:
Gate 4, 2026-07-03, приёмку подтвердил владелец, 49 passed). Gate 5 открывается
**с фундамента**: под-гейт **5.0 — контракт слоя петли (D-13)** ложится ДО вёрстки
экранов. Этот под-шаг — **5-pre-a (только DECISIONS, кода ноль)**.

## Замер контекста
- Старт сессии: ~55K токенов (оценка: системный промпт + CLAUDE.md +
  SESSION_HANDOVER.md + 4 управляющих документа + handover/gate-4.md; прямого
  счётчика нет).
- На момент записи: ~62K токенов (после чтения документов + запись D-13 + этот
  handover; кода не трогали).
- Причина записи: открытие Gate 5.
- Порог 150K / потолок 170K — запас большой, до потолка далеко.

## Цель gate
Gradio-интерфейс из трёх экранов: **тема → уточняющие вопросы → прогресс+отчёт**.
Переключение экранов через `visible` + `gr.State`. На экране прогресса —
накопительный лог + счётчики (поисков / итераций / score); промежуточные
результаты оценщика видны. Email-галочка как демонстрация (D-05, без реальной
отправки).

## Критерии закрытия (чек-лист дословно из WORKFLOW.md, секция Gate 5)
- [ ] Три группы компонентов, переключение видимости через `gr.State`.
- [ ] Экран 2: 3 статичных текстбокса (Подход А, без `@gr.render`), email-галочка.
- [ ] Экран 3: накопительный лог + счётчики (поисков / итераций / score).
- [ ] Кастомная тема + CSS только через свои `elem_classes` (безопасно).
- [ ] Ручная проверка флоу от начала до конца.

**Тесты:** ручная проверка (автотесты Gradio-фронта не пишем — не стоит усилий,
RULES §4). Детерминированная обвязка слоя петли (генератор/`failed`/бюджет) —
покрывается юнит/E2E на под-гейте 5.0.

## Gate 5 открывается под-гейтом 5.0 — контракт слоя петли (D-13) ДО экранов
Вёрстка экранов НЕ начинается, пока не закрыт **под-гейт 5.0** — контракт слоя
петли под UI. Он ложится ПОД экраны: определяет, как петля отдаёт прогресс наружу
(генератор), как считается бюджет на весь запрос (per-loop) и как поток завершается
при падении (`failed`). Класть экраны поверх неопределённого контракта = вскрытие
контракта посреди вёрстки (урок 4d — контракт потока правится один раз).

**D-13 (ПРИНЯТО, 2026-07-03, владелец подтвердил)** — три решения одним пакетом:
- **D-13.1** — бюджет per-loop: один `ResearchContext` на весь запрос (не свежий на
  доработку). Честный предохранитель D-03. **Закрывает DEFERRED #6.**
- **D-13.2** — канал-генератор: петля → `AsyncIterator[ProgressEvent]`
  (`stage`/`payload`/`report`), поток всегда завершается одним `final` с `ReportData`
  (сохраняет E2E-контракт D-06/D-12). Отклонён callback.
- **D-13.3** — failed-терминал: `try/except` вокруг ядра; есть оценённая версия →
  graceful exit лучшей по score; нет → событие `stage="failed"`. Зависит от D-13.2.
  **Закрывает DEFERRED #7.**

### Карта под-шагов 5.0 (атомарная приёмка по каждому, владелец принимает каждый)
- **5-pre-a — DECISIONS (этот шаг).** Запись D-13 в DECISIONS.md + открытие
  gate-5.md. **Кода ноль.** Границы: только `DECISIONS.md` + `handover/gate-5.md`.
- **5-pre-b — подъём `ResearchContext`.** Вынести `ResearchContext` из
  `run_research` наружу, чтобы бюджет был общим на весь конвейер (D-13.1), а
  счётчик поисков доступен наружу для UI-лога.
- **5-pre-c — петля → генератор + `failed`.** Перевести слой петли с колбэка
  `on_iteration` на `AsyncIterator[ProgressEvent]` (D-13.2), завершение всегда
  одним `final` с `ReportData`; обернуть ядро в `try/except` с терминалом `failed`
  (D-13.3). Правит принятые 4b + 4c.
- **5-pre-d — E2E под поток.** Расширить/адаптировать E2E-контракт под генератор
  (труба остаётся зелёной, терминал `final`/`failed` проверяется на моках).

**Вёрстка экранов Gate 5 (три группы, `gr.State`, лог+счётчики) — ПОСЛЕ закрытия
5.0.**

## Сделано в этой сессии
- **5-pre-a — DONE (ждёт приёмки владельца).** D-13 записан в DECISIONS.md
  (статус ПРИНЯТО, дата 2026-07-03, владелец подтвердил) — три под-пункта
  (13.1 per-loop / 13.2 генератор / 13.3 failed) с причинами, отклонёнными
  вариантами, названной тенью принятого per-loop и зафиксированным переплетением
  (подъём контекста обслуживает и бюджет, и счётчик UI; генератор несёт
  score/feedback + счётчик + failed). Открыт этот handover. **Кода не трогали.**
- **5-pre-b — DONE (ждёт приёмки владельца).** `src/run_research.py`: `run_research`
  получил необязательный параметр `context: ResearchContext | None = None`. Внутри
  `ctx = context if context is not None else ResearchContext()`, дальше
  `Runner.run(..., context=ctx, ...)` как раньше. Дефолт НЕ изменён — вызов без
  аргумента создаёт свежий контекст (полная обратная совместимость; существующие
  тесты держатся БЕЗ правок). Цель — дать вызывающему (будущей петле-генератору
  5-pre-c) передать ОДИН `ResearchContext` на весь запрос: техническая основа
  per-loop (D-13.1) и подъёма счётчика поисков наружу для UI. Сам per-loop здесь
  ещё НЕ включён — только возможность в сигнатуре ядра; петля/менеджер/предохранители/
  E2E НЕ тронуты. Новый юнит-тест `tests/test_run_research_context.py`: переданный
  `context` используется прогоном (identity `is`), поиски списываются в НЕГО, а не
  в свежий (на моках, без ключа).
- **5-pre-c — DONE (ждёт приёмки владельца).** Сердце D-13 — петля переоформлена из
  колбэка в генератор, включён per-loop, заложен failed-терминал. `src/evaluator_loop.py`:
  * **И-1 генератор (D-13.2).** `research_with_evaluation` теперь чистый
    `async def -> AsyncIterator[ProgressEvent]`. `ProgressEvent(stage, payload, report)`
    (frozen dataclass). Стадии: `research_started` / `draft_ready` / `evaluation`
    (payload `round`/`score`/`feedback`/`missing_topics`/`searches_used`) /
    `revision_started` / `final` / `failed`. Промежуточные события `report=None`.
    Колбэк `on_iteration`/`ProgressCallback`/`_emit` УДАЛЁН — роль взял поток событий.
  * **И-2 терминал (D-13.2).** Поток ВСЕГДА кончается ровно одним терминалом:
    `final` с НЕПУСТЫМ `report: ReportData` (не None — за это цепляется E2E
    `isinstance`). `final`/`failed` взаимоисключающи.
  * **И-3 per-loop (D-13.1).** ОДИН `ResearchContext()` создаётся на весь запрос и
    передаётся в КАЖДЫЙ `run_research(brief, context=ctx)` — и первый прогон, и
    каждую доработку (проверено глазами: доработка получает ТОТ ЖЕ `ctx`).
    `ctx.searches_used` идёт в payload (задел под счётчик UI).
  * **И-4 failed (D-13.3).** Тело обёрнуто в `try/except`: при падении есть
    оценённая версия → graceful exit `final` с лучшей по score (та же ветка, что
    лимит); нет ни одной → терминал `failed` (`report=None`, сообщение в
    `payload["message"]`).
  * **Старые свойства сохранены:** порог `>= 7` (SCORE_THRESHOLD), `MAX_ROUNDS=2`,
    `while not passed and rounds < MAX_ROUNDS`, graceful exit возвращает ЛУЧШУЮ по
    score, тай-брейк `>` (ничья → ранняя). Тесты проверяют ИХ ЖЕ.
  * **Дренер `run_loop_to_report(brief) -> ReportData`** (вариант C, ПРИНЯТ
    владельцем): каноническая свёртка потока к отчёту — `final` → `report`, `failed`
    → исключение с сообщением из payload (НЕ None). **Живёт в `evaluator_loop.py`**
    рядом с генератором и контрактом стадий (обоснование: свёртка ЭТОГО потока, знает
    про терминалы; отдельный модуль был бы слоем без необходимости, RULES §6). Его
    зовут finalizer (сейчас), E2E (5-pre-d), UI (вёрстка) — знание «как достать report
    из потока» в ОДНОЙ точке.
  * **finalizer-стык поправлен СЕЙЧАС (в границах 5-pre-c, раз конверсия его
    затронула — поднято владельцу, вариант C принят).** `src/finalizer_agent.py`:
    `research_and_finalize` зовёт `run_loop_to_report(brief)` вместо
    `research_with_evaluation(brief)` (своп импорта + вызова + докстринг). Тесты
    стыка 4d остались зелёными (финализатор получает тот же `ReportData`, просто
    через дренер).
  * Тесты `tests/test_evaluator_loop.py` переписаны под генератор: 4 старых сценария
    (порог / лимит / лучшая-по-score / тай-брейк) проверяют ТЕ ЖЕ свойства через
    итерацию потока + терминал; +`failed`-терминал (падение без оценённой версии
    → ровно один `failed` без report); +graceful-`final` (падение ПОСЛЕ первой оценки
    → ровно один `final` с непустым отчётом + `payload["graceful"] is True`, `failed`
    в потоке нет); +2 теста дренера (happy → ReportData; failed → исключение, не None).
    Хелпер `_terminal` держит И-2 (ровно один терминал, последний).
  * **Уточнение семантики терминалов (по итогу 5-pre-c, вариант 1 ПРИНЯТ владельцем).**
    Реализация обнажила гибрид «сбой прогона + валидный отчёт на руках» (падение на
    доработке при уже оценённой первой версии). Достройка D-13.2 (не смена решения):
    `final` = «есть оценённый непустой report» двумя путями — (а) нормальный выход,
    (б) graceful после сбоя; `payload["graceful"]: bool` — записанный контракт флага
    (отсутствует/False → путь а, True → путь б); граница `final`/`failed` — по НАЛИЧИЮ
    оценённой версии, не по тяжести сбоя. Дренер флаг СОЗНАТЕЛЬНО игнорит (любой
    `final` = успех) — комментарий в дренере со ссылкой на D-13.2. Код эмиссии
    `final`/`failed` НЕ менялся — уже реализовывал вариант 1; правки 5-pre-c-addendum:
    запись контракта (DECISIONS) + покрытие (graceful-тест) + комментарий дренера.
  * **НЕ тронуты:** менеджер/предохранители/ядро (`run_research` готов с 5-pre-b),
    E2E (5-pre-d), сам механизм handoff-шлюза.
- **5-pre-d — DONE (ждёт приёмки владельца). Под-гейт 5.0 ЗАКРЫТ.** E2E мигрирован на
  контракт-генератор D-13, зелёный воскрешён. `tests/test_e2e_pipeline.py`:
  * **happy-path — через ДРЕНЕР** `run_loop_to_report(brief)` (выбор обоснован:
    каноническая свёртка потока к отчёту — та же поверхность, что зовут finalizer/UI;
    E2E проверяет реальный контракт потребления, а не тест-локальную итерацию).
    Прежние структурные assert'ы держатся (`isinstance ReportData`, `short_summary`,
    `markdown_report`, `follow_up_questions: list`); труба прошла план→поиск→писатель→
    оценщик (`.ran(...)`).
  * **revision-path (fail→pass) — ИТЕРАЦИЕЙ потока напрямую** (как будет делать UI):
    инвариант терминала (ровно один `final` с непустым `ReportData`, `failed` в потоке
    нет) + наблюдаемость (>= 1 событие `evaluation` с ключами `score`/`feedback` в
    payload, D-13.2; содержание feedback НЕ проверяем, RULES §4) + multi-round сборка
    (`ManagerAgent`/`EvaluatorAgent` по два прогона — доработка через ТОТ ЖЕ per-loop
    ctx, D-13.1). Так E2E покрывает ОБА способа потребления петли (дренер + прямой
    поток) — оба реальные потребители D-13.
  * **Мок-слой (`_mocks.py`) НЕ тронут.** Канон `EvaluatorAgent → EvaluationFeedback`
    (4c) переиспользован как есть; per-loop shared-ctx нюанса в диспетчере не всплыло —
    два прогона тратят 2 поиска из бюджета 12 (сценарная модель делает 1 поиск на
    прогон), запас есть, диспетчер править не пришлось.
  * **НЕ тронуты:** петля/дренер/finalizer (5-pre-c принят), механика, мок-слой.

### Вёрстка экранов Gate 5 (после закрытия 5.0). Карта 5a→5c
- **5a — каркас трёх экранов + навигация** ← ЭТОТ ШАГ. Структура + переключение,
  БЕЗ подписки на поток (5b), БЕЗ провокатора (5c).
- **5b — живое наполнение экрана 3**: подписка на поток `ProgressEvent` (накопительный
  лог + счётчики поисков/итераций/score, счётчик версий N/M — D-14.1), три плашки
  терминала (`final`/graceful/`failed` — D-14.2) + «Новое исследование» на любом,
  финал через `research_and_finalize`; подстановка текста вопросов clarifier на экране
  2. **См. «Вход 5b» ниже — обязательные правки к D-14.1 и чек-лист приёмки.**
- **5c — провокатор терминалов** (мок-режим/тестовый бриф/env) в приёмочной обвязке,
  НЕ в живом UI (D-14.3); ручная приёмка прогоняет все три исхода.

- **5a — DONE (ждёт РУЧНОЙ приёмки владельца).** Каркас трёх экранов Gate 5 в
  **`app.py`** (точка входа — обоснование: app.py уже заявлен как Spaces-вход (Gate 6),
  его докстринг обещал реальный UI на Gate 5; легаси `src/deep_research.py` не тронут;
  D-11 — внутренние имена src/ не трогаются). `gr.Blocks` тема **sky** (D-04), три
  группы `gr.Column`, переключаемые через **`gr.State` + `visible`**:
  * **Экран 1 — тема:** textbox + кнопка «Начать».
  * **Экран 2 — уточнения (Подход А, D-04):** РОВНО 3 статичных `gr.Textbox` (НЕ
    `@gr.render`), email-`gr.Checkbox` (D-05 — демо, без реальной отправки, адрес НЕ
    хардкодится), кнопка «Исследовать». Сверху — резюме-строка темы (контекст).
  * **Экран 3 — прогон:** ПУСТОЙ каркас с плейсхолдерами («здесь пойдёт прогон/отчёт —
    5b»); двухколоночная раскладка заложена структурно `gr.Row`(scale=1 телеметрия /
    scale=2 отчёт) БЕЗ данных; сверху — резюме-строки свёрнутых экранов 1–2 (D-14.4,
    БЕЗ «Изменить»). Кнопка «Новое исследование» — навигационный сброс к экрану 1
    (терминал-привязанный сброс D-14.2 — на 5b).
  * Навигация: `start_btn` (1→2), `research_btn` (2→3, только переключение — петля НЕ
    подключена), `new_research_btn` (сброс→1); переходы двигают `gr.State` и `visible`,
    экраны 1–2 сворачиваются в резюме (не исчезают). CSS только через свои
    `elem_classes` (`mad-summary`/`mad-placeholder`).
  * **НЕ сделано на 5a (по границам):** подписка на поток петли, счётчики/лог/версия
    N/M, плашки терминала, провокатор, текст вопросов clarifier. Петля/дренер/
    finalizer/предохранители НЕ тронуты; автотесты Gradio НЕ писались (RULES §4 —
    фронт на ручной проверке). `build_ui()` конструируется без ошибок (smoke: возвращает
    `Blocks`); 54 passed держатся (UI-код их не касается).

## Вход 5b — обязательные правки к D-14.1 (всплыли при ревью макета)
На 5a лога ещё нет — здесь только ЗАПИСАНО, не реализовано. При наполнении экрана 3
(5b) обязательно:
1. **Терминология «версия/черновик» относится к АРТЕФАКТУ-ОТЧЁТУ, не к волнам поиска.**
   Волны поиска сохраняют свой словарь: в логе «волна N: X/Y сводки», а НЕ «версия N».
   Иначе лента противоречива (счётчик «Версия N» существовал бы до готовности версии).
   Счётчик версий N/M (D-14.1) считает именно версии отчёта (`round + 1`), поиски в
   логе — своим словарём.
2. **Пара терминов «версия» (артефакт) + «доработка» (процесс) — осознанная,**
   зафиксировать в UI-копирайтинге: версия — что получилось (артефакт-отчёт),
   доработка — что делается между версиями (процесс оценки→ревизии).

**Чек-лист ручной приёмки 5b (добавить к прогону):**
- проверка на УЗКОМ экране: колонки складываются, лог не должен «хоронить» отчёт
  (возможно `gr.Accordion` для лога);
- **autoscroll лога** — лента прилипает к низу по мере поступления событий;
- **таймер прогона** — «Идёт исследование · M:SS»;
- **стриминг как увидит Spaces**, а не только `python app.py`: прогнать без
  `inbrowser`, с явной `.queue()`. Генераторы идут разными транспортами — локальный
  `.queue()` на Windows vs SSE на Spaces; классический симптом рассинхрона — лог живой
  локально, но приходит ПАЧКОЙ в конце на Spaces. Полная проверка живости лога на
  настоящем Space — Gate 6 (**DEFERRED #9**, зазор среды Windows→Spaces).

## НЕ сделано / точка остановки
**Под-гейт 5.0 (контракт слоя петли, D-13) ПОЛНОСТЬЮ ЗАКРЫТ и запушен.** Вёрстка
началась: **5a (каркас + навигация) — DONE, ждёт ручной приёмки.**

**Точка остановки — приёмка 5a владельцем** (кликнуть флоу локально, см.
«Как проверить 5a» ниже). НЕ начаты: **5b** (живое наполнение экрана 3 — поток,
счётчики, версия N/M, плашки терминала, финал; + правки D-14.1 из «Вход 5b») и **5c**
(провокатор терминалов). Петля/дренер/finalizer готовы (5.0) и ждут подключения на 5b.

## Как проверить 5a локально (ручная приёмка)
Запуск: **`python app.py`** (из корня; откроет браузер, `inbrowser=True`). Ключ
OpenAI НЕ нужен — петля на 5a не вызывается. Что увидеть по шагам:
1. **Экран 1 (тема):** заголовок «MultiAgency DeepResearch», поле «Что исследуем?»,
   кнопка «Начать». Ввести тему → «Начать».
2. **Экран 2 (уточнения):** сверху приглушённая строка «Тема: …» (резюме экрана 1);
   РОВНО 3 текстбокса вопросов; чекбокс email-копии (демо); кнопка «Исследовать».
   Заполнить (можно частично) → «Исследовать».
3. **Экран 3 (прогон):** сверху резюме-строки свёрнутых экранов 1–2 (тема / уточнения /
   email — БЕЗ кнопки «Изменить»); две колонки — слева «Телеметрия» (уже плейсхолдер),
   справа «Отчёт» (плейсхолдер); кнопка «Новое исследование» → возврат на экран 1.
Экраны переключаются на месте (1–2 сворачиваются в резюме, не пропадают целиком).
Живого прогона/лога/отчёта на 5a НЕТ — это плейсхолдеры (наполнит 5b).

## Тесты
- Написаны/переписаны: `tests/test_e2e_pipeline.py` (5-pre-d) — 2 E2E под генератор
  (happy через дренер / revision итерацией потока). `tests/test_evaluator_loop.py`
  (5-pre-c) — 8 тестов под генератор (4 старых свойства + failed + graceful-final +
  2 дренера). `tests/test_run_research_context.py` (5-pre-b) — переданный `context`
  используется прогоном. Всё на моках `Runner.run`, без ключа.
- Проходят: **54 passed, 0 failed, 0 xfailed** (`python -m pytest -v`). 52 от 5-pre-c
  + 2 воскресших E2E.
- E2E статус: **ЗЕЛЁНЫЙ** — труба идёт через контракт-генератор D-13 (happy через
  дренер, revision через прямой поток с проверкой терминала и наблюдаемости итераций).
  Инвариант «E2E зелёный» (RULES §4) восстановлен; красноты не осталось.

## Deferred, поднятые/заходящие в этот gate
**Заходящие в Gate 5 (по реестру WORKFLOW.md):**
| # | Вопрос | Привязан к gate | Решение |
|---|--------|-----------------|---------|
| 6 | Семантика бюджета поисков per-run vs per-loop | Gate 5 (вход, пакет D-13) | решён пакетом D-13.1 (per-loop) прямо здесь |
| 7 | Graceful-обработка падения прогона (`MaxTurnsExceeded`/сеть/API) | Gate 5 | решён пакетом D-13.3 (failed-терминал) прямо здесь |

**Других заходящих в Gate 5 нет — подтверждаю явно:** по реестру WORKFLOW.md #1 и
#5 привязаны к Gate 6; #2/#3/#4 закрыты. На Gate 5 приходят только #6 и #7, оба
закрываются пакетом D-13 на под-гейте 5.0.

**Порождённые в Gate 5 DEFERRED:**
| # | Вопрос | Возник в | Привязан к | Статус |
|---|--------|----------|------------|--------|
| 8 | Отмена автономного прогона на лету (cancel): контракт бюджета/трейса/частичных результатов при прерывании; для MVP не нужен (сброс — «Новое исследование» на терминале, D-14.4) | Gate 5 (проектирование вида, D-14) | после Gate 6 | открыт |

## Решения, требующие владельца
- **D-13 — ПРИНЯТО** (2026-07-03, владелец подтвердил). Записано в DECISIONS.md,
  включая уточнение семантики терминалов (вариант 1, по итогу 5-pre-c).
- **Под-гейт 5.0 (5-pre-a/b/c/d) — ПРИНЯТ и запушен** (`515cc1d`/`636a1f3`/`065baa6`/
  `64aa43e`). Пакет D-13 реализован, E2E зелёный.
- **D-14 — ПРИНЯТО** (2026-07-03, владелец подтвердил, запушено `243744e`). Контракт
  вида экрана 3 (UI над потоком D-13): счётчик версий N/M (D-14.1), три плашки
  терминала + сброс на любом (D-14.2), провокатор терминалов вне живого UI (D-14.3),
  свёрнутые экраны как резюме без «Изменить» (D-14.4). Записано в DECISIONS.md;
  критерии Gate 5 в WORKFLOW.md дополнены; DEFERRED #8 (отмена на лету) заведён.
- **5a — ждёт РУЧНОЙ приёмки** (каркас трёх экранов + навигация; петля НЕ подключена).
  Проверка — `python app.py`, см. «Как проверить 5a локально». Автотестов фронта нет
  (RULES §4). Открытых решений, требующих владельца, на 5a нет — только приёмка вида.

## Следующий шаг
Приёмка **5a** владельцем (кликнуть флоу тема→вопросы→прогон локально). После неё —
**5b: живое наполнение экрана 3** под D-14 + D-13: подписка на поток `ProgressEvent`
(лог + счётчики поисков/итераций/score, версия N/M), три плашки терминала
(`final`/graceful/`failed`) + «Новое исследование» на любом, финал через
`research_and_finalize`, текст вопросов clarifier на экране 2. **ОБЯЗАТЕЛЬНО учесть
«Вход 5b»** (терминология версия=артефакт / поиски=волны; чек-лист: узкий экран,
autoscroll лога, таймер). Затем 5c (провокатор терминалов вне UI). Ручная приёмка
Gate 5 прогоняет все три исхода.
