# Handover — Gate 3: Автономный менеджер

## Тип
[x] Открытие gate   [ ] Закрытие gate   [ ] Перенос сессии (gate не закрыт)

## Замер контекста
- Старт сессии: 45K токенов (оценка: системный промпт + CLAUDE.md + 8
  управляющих/кодовых файлов; прямого счётчика нет).
- На момент записи: ~45K токенов.
- Причина записи: открытие Gate 3.
- Порог 150K / потолок 170K — большой запас, риска упереться на этом gate нет.

## Цель gate
Менеджер-агент оркестрирует инструменты сам (agents-as-tools, D-01): принимает
решения о числе и направлении поисков, включая динамическую вторую волну по
пробелам в данных. Ядро зоны 2 из CLAUDE.md.

## Критерии закрытия (чек-лист дословно из WORKFLOW.md)
- [x] `manager_agent` с инструментами: план, поиск, писатель. _(3a)_
- [x] Промпт менеджера: бюджет поисков, инструкция про вторую волну по пробелам. _(3a)_
- [x] E2E happy-path ЗЕЛЁНЫЙ: `бриф → план → поиски → отчёт` на моках. _(3c)_
- [x] Юнит-тест: менеджер уважает бюджет поисков (не превышает лимит). _(3b)_

**Все четыре критерия WORKFLOW фактически закрыты.** Оставшийся под-шаг **3d
(граница D-09)** — это НЕ критерий из списка, а вынесенная граница (см. ниже).

**Тесты:** unit (соблюдение бюджета) + E2E (контракт трубы).
**Не тестируем:** конкретные решения LLM о запросах — недетерминированны.

## Порядок шагов Gate 3 (атомарная приёмка, владелец принимает каждый)
- **3a — `manager_agent` + промпт** (бюджет, вторая волна). ← этот шаг.
- **3b — Подключение предохранителей:**
  `Runner.run(manager, brief, context=ResearchContext(), max_turns=MAX_TURNS)` +
  юнит-тест соблюдения бюджета менеджером.
- **3c — Оживление E2E:** снять `xfail` (strict=True → xpass упадёт как сигнал),
  убедиться, что труба зелёная; тут же решить DEFERRED #4, если всплывёт.
- **3d — Граница D-09:** обсудить с владельцем, где ложится handoff на
  `finalizer_agent` (стык Gate 3 / Gate 4), решить, входит ли в Gate 3 вообще.

## Сделано в этой сессии
- **3a — DONE.** `src/manager_agent.py`: agents-as-tools (D-01), три готовых
  инструмента (`plan_web_searches` / `run_parallel_searches` / `write_report`,
  не переписаны), промпт с бюджетом поисков + реакцией на
  `SEARCH_BUDGET_EXHAUSTED`/`PARTIAL` + динамической второй волной по пробелам.
  Без `handoffs`, без подключения к рантайму (`Runner.run`). Принят владельцем.
- **3b — DONE.** `src/run_research.py` — тонкая точка входа:
  `Runner.run(manager, brief, context=ResearchContext(), max_turns=MAX_TURNS)` →
  `final_output_as(ReportData)`. Юнит-тест соблюдения бюджета менеджером
  (`tests/test_run_research_budget.py`): жадный сценарий рвётся за лимит, инвариант
  держится. F-string-стык бюджета закрыт фиксацией дефолта (промпт и рантайм оба
  от `DEFAULT_SEARCH_BUDGET`). Принят владельцем, опубликован.
- **3c — DONE.** E2E оживлён честно: на менеджере `output_type=ReportData` (B1,
  боевая правка), сценарная модель в мок-слое проводит менеджера
  план→поиск→писатель→финал настоящим `Runner`; `xfail` снят. **40 passed,
  0 xfailed.** Тело E2E-контракта не тронуто (снят только маркер). Принят
  владельцем, опубликован.

### Разворот решения B2 → B1 (на 3c)
- Сначала владелец принял **B2** (`tool_use_behavior=StopAtTools(["write_report"])`)
  — «вывод писателя терминален».
- При реализации (разбор исходника SDK) выяснилось: `.as_tool()` **стрингифицирует**
  вывод суб-агента через `text_message_outputs`, поэтому `StopAtTools` захватил бы
  `str`, а не `ReportData` — контракт `isinstance(report, ReportData)` не
  выполняется. **B2 механически не зеленит трубу.**
- Владелец **отклонил B2, принял B1** (`output_type=ReportData` на менеджере):
  менеджер сам эмитит финальную структуру, SDK валидирует его финальное сообщение.
  Эмпирический прогон голого B2 владелец счёл излишним.

## НЕ сделано / точка остановки
Из пяти пунктов входа SESSION_HANDOVER §8 закрыты четыре (3b — предохранители,
3c — оживление E2E, DEFERRED #4, тест бюджета). **Остался один:**
- **Граница D-09** — где ложится handoff на `finalizer_agent`. Критическое
  решение владельца (RULES §1), НЕ решаю сам. Приносится на шаге **3d**. Надзор
  (владелец) заранее отметил: D-09 логически завязан на evaluator-петлю (Gate 4),
  поэтому полноценный финализатор физически может лечь на стык Gate 3 / Gate 4.
  Вводная от B1 (см. «Решения…» ниже): терминал прогона — уже `ReportData` от
  менеджера, что сужает пространство 3d.

**Пункт внимания к 3b (f-string бюджета) — ЗАКРЫТ на 3b.** Промпт менеджера берёт
бюджет из `DEFAULT_SEARCH_BUDGET`, `run_research` создаёт `ResearchContext()` от
того же дефолта без параметра бюджета → промпт и рантайм не расходятся.
Настраиваемый бюджет не вводился (смена контракта, при надобности — ПРЕДЛОЖЕНО).

## Тесты
- Написаны: + `tests/test_run_research_budget.py` (3b) — соблюдение бюджета.
- E2E `tests/test_e2e_pipeline.py` — оживлён (3c), `xfail` снят.
- Проходят: **40 passed, 0 xfailed** (на моках, без ключа).

## Deferred, поднятые/заходящие в этот gate
| # | Вопрос | Привязан к gate | Статус |
|---|--------|-----------------|--------|
| 4 | Двойник RunResult в мок-слое при извлечении вывода `.as_tool()` менеджером | Gate 3 | **РЕШЁН (3c)** — `.as_tool()`-извлечение идёт через `.new_items` + `text_message_outputs`, не через `final_output`; двойник в `_mocks.py` достроен `.new_items` с настоящим `MessageOutputItem` (честная эмуляция SDK-пути, не заглушка); менеджер проводится `ScenarioManagerModel` через настоящий `Runner`. |

## Решения, требующие владельца
- **Граница D-09** (handoff на `finalizer_agent`): входит ли в Gate 3 или на стык
  с Gate 4. Приносится на шаге 3d. Claude Code не решает сам (RULES §1).
- **Модель менеджера** — `gpt-4o`: **ПРИНЯТО владельцем как дефолт**. Тюнинг, не
  архитектура; пересмотр возможен на Gate 6.

### Следствия для DECISIONS.md (ПРЕДЛОЖЕНО — занести отдельным docs-шагом уровня проекта)
> Уточнения к уже принятым D; здесь только зафиксированы, в `DECISIONS.md` НЕ
> вносятся Claude Code сам — выносятся владельцу (RULES §1).
- **К D-01 — ПРЕДЛОЖЕНО.** B1 реализует «менеджер владеет финальным ответом»
  буквально: финал эмитит менеджер через `output_type=ReportData`. Прежнее
  интерпретационное натяжение (обсуждавшееся под B2) снято.
- **К D-09 — ПРЕДЛОЖЕНО (вводная к 3d).** При B1 терминальный результат прогона —
  `ReportData` от менеджера. Финализатор (D-09) на Gate 4 получает готовую
  структуру, а не сырой выход — это сужает пространство обсуждения границы 3d.

## Следующий шаг
**3d — граница D-09** (единственный оставшийся под-шаг Gate 3): где ложится
handoff на `finalizer_agent`, стык Gate 3 / Gate 4. Обсуждение с владельцем,
отдельный промт. Вводная — следствие B1 выше (терминал = `ReportData`).
