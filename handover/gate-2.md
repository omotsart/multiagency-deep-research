# Handover — Gate 2: Агенты-инструменты + предохранители

## Тип
[x] Открытие gate   [ ] Закрытие gate   [ ] Перенос сессии (gate не закрыт)

## Замер контекста
- Старт сессии: ~20K токенов (системный промпт + инструменты + CLAUDE.md, до чтения документов).
- На момент записи: ~50K токенов (оценка: CLAUDE + WORKFLOW + DECISIONS + RULES + HANDOVER + gate-1 + вывод инструментов).
- Порог предупреждения (150K, RULES §2 / D-07) и потолок (170K) не достигнуты — запас большой, Gate 2 открывается не впритык.
- Причина записи: открытие Gate 2.

## Цель gate
Существующие агенты (планировщик, поиск, писатель) обёрнуты как инструменты
(`.as_tool()`) для будущего автономного менеджера. Заложены предохранители
автономности (бюджет поисков, `max_turns`). Поднят каркас E2E (мок-слой + скелет
happy-path теста). Внутренние имена агентов/файлов `src/` НЕ меняются (D-11).

## Критерии закрытия (чек-лист из WORKFLOW.md)

### Под-gate 2a — Планировщик как инструмент
- [ ] `planner_agent` доступен менеджеру через `.as_tool()`.
- [ ] Юнит-тест схемы `WebSearchPlan`.

### Под-gate 2b — Параллельный поиск как инструмент
- [ ] Обёртка «запусти N поисков параллельно» (сохранён `asyncio.as_completed`).
- [ ] Юнит-тест: обёртка возвращает список сводок, ошибки одного поиска не рушат остальные.

### Под-gate 2c — Писатель как инструмент
- [ ] `writer_agent` доступен через `.as_tool()`.
- [ ] Юнит-тест схемы `ReportData`.

### Под-gate 2d — Предохранители
- [ ] Лимит поисков в `RunContextWrapper` (бюджет); поисковый tool отказывает при исчерпании.
- [ ] `max_turns` задан в `Runner.run()`.
- [ ] Юнит-тесты предохранителей (лимит поисков срабатывает; счётчик корректен).

### Под-gate 2e — Каркас E2E
- [ ] Мок-слой для LLM-вызовов агентов.
- [ ] Скелет happy-path теста (пока может быть `xfail`, пока нет менеджера).

**Тесты gate:** unit (схемы, обёртка поиска, предохранители) + каркас E2E.
**E2E-контракт** (D-06): happy-path на моках закладывается здесь, оживает на Gate 3.

## DEFERRED, заходящие в этот gate (принесены из реестра WORKFLOW, RULES §3)
| # | Вопрос | Статус на входе | Действие в Gate 2 |
|---|--------|-----------------|-------------------|
| 2 | Инициализировать git-репозиторий (первым шагом Gate 2) | открыт | **выполнено на открытии gate** — git init под slug из D-11, первый коммит состояния (закрытый Gate 1 + документы) |
| 3 | Финальное название проекта | закрыт → D-11 | slug `multiagency-deep-research` применён при git init; переименование касается только обёртки, не имён `src/` |

Оба зафиксированы как заходящие в Gate 2. #3 архитектурно уже закрыт (D-11), в gate реализуется только его следствие (slug для репозитория).

## Сделано в этой сессии
- Открыт Gate 2, создан настоящий handover.
- **git init** под финальным slug `multiagency-deep-research` (D-11).
- `.gitignore` проверен — уже покрывает `.env`, `.venv/`, `__pycache__/`,
  `.pytest_cache/` и пр.; изменений не потребовалось.
- Первый коммит всего текущего состояния (закрытый Gate 1 + документы). `.env` и
  секреты в коммит НЕ попали (проверено по `git status` / составу коммита).

## НЕ сделано / точка остановки
- Код под-gates 2a–2e не начат (по условию задачи: сначала вход в gate +
  инфраструктура, обёртки `.as_tool()` — отдельными атомарными шагами).
- Следующий конкретный шаг: под-gate 2a (планировщик как инструмент) — по одному
  под-gate за раз, приёмка атомарная.

## Тесты
- Написаны: в этой сессии новых нет (инфраструктурный вход в gate).
- Проходят: наследованные с Gate 1 — 12 passed (не перепрогонялись в этой сессии).
- E2E статус: не применимо пока (каркас появляется в под-gate 2e).

## Deferred, поднятые в этом gate
| # | Вопрос | Привязан к gate |
|---|--------|-----------------|
| — | — | — |

## Решения, требующие владельца
- Критических решений, требующих «принято», на этом шаге нет: git init и slug —
  следствия уже принятых D-11 и deferred #2. Архитектурные решения по обёрткам/
  предохранителям будут возникать в под-gates 2a–2d и, если выйдут за рамки уже
  принятого, помечаются ПРЕДЛОЖЕНО (RULES §1).

## Следующий шаг
Под-gate 2a: обернуть `planner_agent` через `.as_tool()` + юнит-тест схемы
`WebSearchPlan`. Отдельным шагом, атомарная приёмка.
