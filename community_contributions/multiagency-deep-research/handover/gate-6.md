# Handover — Gate 6: Деплой на Hugging Face Spaces

## Тип
[ ] Открытие gate — **[x] ДА**   [ ] Закрытие gate   [ ] Перенос сессии (gate не закрыт)

Финальный gate проекта. Открывается процедурой входа (RULES §3): перенос критериев
из WORKFLOW.md §Gate 6, диагностика готовности, подъём заходящих DEFERRED. **Реализация
деплоя НЕ начата** — ждём решений владельца по трём DEFERRED и утверждения карты под-шагов.

## Замер контекста
- Старт сессии: **~68K токенов** (оценка: системный промпт + CLAUDE.md + пять управляющих
  документов WORKFLOW/RULES/HANDOVER/DECISIONS + handover/gate-5.md + диагностика; прямого
  счётчика нет). Порог 150K / потолок 170K — **запас большой**, стартуем низко.
- На момент записи: **~72K токенов** (после чтения документов, диагностики и записи этого handover).
- Причина записи: **открытие Gate 6**.

## Цель gate
Приложение **живёт на Hugging Face Spaces**, доступно по публичному URL; happy-path
(тема → 3 уточнения → автономный прогон → отчёт) проходит **вручную на живом Space**.
Последний gate — после него проект в деплое.

## Критерии закрытия (дословно из WORKFLOW.md §Gate 6)
- [ ] `app.py` в корне как точка входа.
- [ ] `requirements.txt` с зафиксированной версией Gradio.
- [ ] `OPENAI_API_KEY` заведён как Secret в настройках Space (не в коде).
- [ ] Захардкоженные адреса email удалены.
- [ ] Копия проекта в `community_contributions/`.
- [ ] Space открывается и проходит happy-path вручную.

**Тесты:** ручная проверка на живом Space.

## Сделано в этой сессии
- **Открытие Gate 6** (handover + диагностика) — предыдущий шаг.
- **6a — решения владельца по DEFERRED + документная гигиена** (этот шаг; кода петли/
  менеджера/финализатора/UI НЕ трогали — только документы и requirements). Детали — в блоке
  «6a — выполнено» ниже. Кратко: A1 PII вариант B + D-15 + чистка цитат; A2 разнос
  requirements с жёсткими пинами; A3 проверка долга журнала (долга нет — D-13/D-14 на месте);
  A4 переписан email-пункт по факту; A5 принятие риска среды #9.

## Диагностика готовности к деплою (состояние по каждому критерию — НЕ правка)

| Критерий | Состояние | Детали |
|----------|-----------|--------|
| `app.py` в корне | **готово (проверить на живом Space)** | Есть, точка входа: `app.py:538` `if __name__ == "__main__": build_ui().queue().launch(inbrowser=True)`. `.queue()` включён (стриминг генераторов). Нюанс Spaces: HF запускает файл как `__main__` — паттерн рабочий; **живость стриминга — только на настоящем Space (#9)**. |
| `requirements.txt`, Gradio запинен | **готово (обновлено в 6a/A2)** | Deploy-стек жёстко запинен (`==`): `openai-agents==0.4.2`, `gradio==5.49.1`, `pydantic==2.11.10`, `python-dotenv==1.2.2` (номера из рабочего окружения — на сверке владельца). Dev/test-депы вынесены в `requirements-dev.txt` (`pytest==9.1.1`, `pytest-asyncio==1.4.0`); `sendgrid==6.12.5` тоже туда (legacy, вне deploy-пути, D-05). Закрывает #1 (при приёмке). |
| `OPENAI_API_KEY` не в коде | **готово** | Нигде не захардкожен и даже не читается вручную — SDK берёт ключ из окружения сам. `git grep OPENAI_API_KEY` по `*.py` — 0 совпадений. Пойдёт как Secret на Space. |
| Захардкоженные email удалены | **готово** | Реальных/личных адресов в коде нет — по факту (grep/ls). **Deploy-путь (`app.py`):** email — только демо-`gr.Checkbox` `app.py:431` («реальная отправка не выполняется»), НЕ подключён к отправке (входит только в reset/clear, не в стрим). `app.py` НЕ импортирует ни `email_agent`, ни `research_manager`. **Legacy:** `src/email_agent.py` существует (`ls` → 1548 B), плейсхолдеры `*@example.com` в `os.environ.get(...)`, но импортируется ТОЛЬКО `research_manager.py`→`deep_research.py` (наследованный UI) — в deploy-путь `app.py` не входит. RULES §5 (личные адреса) соблюдён; D-05 цел. _(Правка A4: прежняя формулировка ссылалась на `email_agent.py:11-12` как на deploy-поведение — неточно: файл существует, но легаси; deploy-поведение — чекбокс в `app.py`.)_ |
| Провокатор на проде | **готово (не помешает)** | `MAD_FORCE_TERMINAL` — env-gated, ленивый импорт `acceptance_provocateur.py`, seam — одна проверка env в `_event_source` (`app.py:258`). В нормальном прогоне (env не задан) не импортируется; кнопок «уронить прогон» в UI нет (D-14.3). |
| `community_contributions/` | **нужна правка** | Папки **НЕТ**. Требует создания + копии проекта (под-шаг деплоя). |
| Тесты | **готово** | `python -m pytest -q` → **54 passed** (0.07s). Состояние Gate 5 не изменилось. |

**Итог диагностики:** из 6 критериев к правке минимум **1** (`community_contributions/` —
создать) + **1** живой (happy-path на Space) + Secret заводится в UI Spaces. Остальные
(`app.py`, `requirements`, ключ, email, провокатор) — **готовы** в репозитории; #1 и #9 —
предмет решений владельца (ниже).

## DEFERRED, заходящие в Gate 6 (RULES §3 — приносятся владельцу как решения, НЕ решаю сам)

**Подтверждаю явно: заходящих в Gate 6 DEFERRED ровно ТРИ — #5, #9, #1.** По реестру
WORKFLOW.md: #2/#3/#4/#6/#7 закрыты; **#8** (отмена на лету) и **#10** (живой per-iteration
черновик) привязаны к «после Gate 6» — в Gate 6 НЕ заходят.

### #5 — PII в git-истории — РЕШЕНИЕ B ПРИНЯТО (6a/A1)
- **Решение владельца: вариант B** — адрес рабочий, экспозиция уже произошла (репо был
  публичным), принято осознанно. История НЕ переписывается, `push --force` запрещён,
  `2fa3339` остаётся точкой истины. Зафиксировано в **D-15 (ПРИНЯТО)**.
- **Что было:** личный email (`[email отредактирован]`, опечатка-запятая) в теле
  `handover/gate-0.md:25,36` + цитаты в `docs/WORKFLOW.md`, `SESSION_HANDOVER.md`,
  `handover/gate-5.md`, `handover/gate-6.md`.
- **Сделано в 6a/A1:** все цитаты адреса в **рабочем дереве** заменены на плейсхолдер
  `[email отредактирован]`, смысл строк сохранён. Проверка: `git grep "motsart@mail"` →
  0 (после правок). В **истории** адрес остаётся (коммиты до `2fa3339`) — сознательно (B).
- **В каких коммитах (история):** `gate-0.md` внесён в `64717a5`; pickaxe
  `git log -S "mail.ru"` → `64717a5` → `219d305` → `81b1ee5` → `2fa3339`.
- **Приватность:** репозиторий **УЖЕ ПУБЛИЧНЫЙ** (анонимный HTTP-запрос → **200**,
  приватный отдал бы 404). Гарантии вычистки из внешних кешей/индексов нет — учтено в B.
- **NB об авторстве:** автор коммитов — noreply-форма
  (`35778795+omotsart@users.noreply.github.com`), чисто; PII была только в *содержимом*.

### #9 — зазор среды Windows → Spaces (главный технический риск деплоя)
- **Что:** разработка велась на **Windows**, деплой — на **Linux/Spaces**. Проверяемо
  **только на живом Space**.
- **Что проверить на Space:** живость SSE-стриминга генераторов (лог идёт в реальном
  времени ИЛИ прилетает пачкой в конце — классический симптом рассинхрона `.queue()`
  Windows vs SSE Spaces); поведение `.queue()` (уже включён, `app.py:541`); пути;
  env-переменные (Secret читается); `inbrowser=True` на Spaces игнорируется (не влияет).
- **Решение (6a/A5): РИСК ПРИНЯТ.** Зазор среды Windows→Spaces принят; проверяется
  **вручную на живом Space** на под-шаге **6c** (стриминг/пути/env), локально не гадаем.
  Записи в DECISIONS не требует (это принятие риска + план проверки, не смена контракта).

### #1 — пины dev-зависимостей — РЕШЕНО (6a/A2): жёсткие пины + разнос deploy/dev
- **Сделано:** dev/test-депы (`pytest==9.1.1`, `pytest-asyncio==1.4.0`) вынесены в новый
  `requirements-dev.txt`; из `requirements.txt` убраны. Весь deploy-стек запинен жёстко
  (`==`): `openai-agents==0.4.2`, `gradio==5.49.1`, `pydantic==2.11.10`,
  `python-dotenv==1.2.2`. `sendgrid==6.12.5` вынесен из deploy в `requirements-dev.txt`
  (legacy email-демо, вне deploy-пути `app.py`, D-05).
- **Номера — из рабочего окружения; список сверен и ПРИНЯТ владельцем.**

## 6a — ПРИНЯТО владельцем (2026-07-05)
- **A1 (#5):** вариант **B** принят (адрес рабочий, публичность уже есть). Цитаты в
  рабочем дереве очищены → плейсхолдер; история сохранена (`2fa3339` — точка истины).
  **D-15 (ПРИНЯТО)** записан в DECISIONS.md; DEFERRED #5 закрыт.
- **A2 (#1):** `requirements.txt` — жёсткие `==` пины deploy-стека; dev/test + legacy
  sendgrid → `requirements-dev.txt`. Список версий сверен и принят. DEFERRED #1 закрыт.
- **A3 (долг журнала): СНЯТ.** `grep "### D-1" docs/DECISIONS.md` показал **D-13 (стр. 100)
  и D-14 (стр. 195) УЖЕ в DECISIONS.md**, статус ПРИНЯТО (2026-07-03, содержание сверено
  владельцем). Премиса A3 («journal кончается на D-12, grep D-13 → 0») была из устаревшего
  среза, не из рабочего дерева. Ничего не трогали (перевод ПРИНЯТО→ПРЕДЛОЖЕНО откатил бы
  принятое решение, RULES §1).
- **A4 (email-диагностика):** пункт переписан по факту (grep/ls). Уточнение: `email_agent.py`
  существует (исходная цитата 11-12 была точна), но легаси/вне deploy-пути; deploy-email —
  демо-чекбокс `app.py:431` (D-05). Премиса «файла нет» была ошибкой — снята владельцем.
- **A5 (#9):** риск среды Windows→Spaces принят, проверяется на живом Space (6c).

## Карта под-шагов Gate 6 (атомарные, приёмка по каждому)
- **6a — решения владельца по #5 / #9 / #1 + документная гигиена. — ПРИНЯТО (2026-07-05).**
  A1–A5: PII вариант B (D-15), разнос requirements, сверка долга журнала, правка email-пункта,
  принятие риска #9. Только документы + requirements; код не трогали.
- **6b — подготовка репо к деплою.** README/заголовок под деплой, конфиг Space (README-хедер HF:
  `sdk: gradio`, `app_file: app.py`, pinned `sdk_version`). Secret-конфиг документируется.
  (PII/requirements уже сделаны в 6a.) 54 passed держатся.
- **6c — деплой на Space + happy-path на живом Space.** Завести `OPENAI_API_KEY` как Secret,
  запушить на HF, прогнать happy-path вручную, проверить живость стриминга (#9).
- **6d — копия в `community_contributions/` + закрытие gate.** Создать папку, положить копию,
  финальная ручная приёмка владельца, запись в журнал WORKFLOW.md.

## Тесты
- Написаны: — (документ; кода не трогали).
- Проходят: **да — 54 passed** (`python -m pytest -q`, 0.07s). Состояние Gate 5 не изменилось.
- E2E статус: **зелёный** (труба через контракт-генератор D-13).

## Следующий шаг
Дождаться **решений владельца по трём DEFERRED** (#5 первым — репозиторий уже публичный)
и **утверждения карты под-шагов**. Реализацию (6b и далее) НЕ начинать до этого. Коммит
этой сессии — только `handover/gate-6.md` (`docs(gate-6): открытие gate`); **push — после
приёмки владельца.**
